from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='Predicting whether a user leaves the forum or not, given his past data.',
    author='RTR',
    license='MIT',
)
